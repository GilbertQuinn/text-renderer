# HTTP listen port (default: 3000)
PORT=3000

# Absolute or relative path to the directory where rendered images are saved.
# Defaults to <project-root>/output
OUTPUT_DIR=./output

# Absolute or relative path to the directory where downloaded font files are cached.
# Defaults to <project-root>/cache/fonts
FONT_CACHE_DIR=./cache/fonts
